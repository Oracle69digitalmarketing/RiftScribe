{
  "name": "riftscribe-lambda",
  "version": "1.0.0",
  "description": "Dependencies for the RiftScribe backend Lambda function.",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
